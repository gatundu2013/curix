export class AuthService {
  async register() {
    /**
     * TODO:
     *
     * Validate OTP
     * check if username exist is db
     * check if phonenumber exist in db
     * hash password
     * store details in database
     * generate Auth tokens
     * return user details
     *
     */
  }
  async login() {}
  async requestOtp() {}
  async resetPassword() {}
  async logout() {}
}
